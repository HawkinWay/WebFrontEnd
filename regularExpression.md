# Regular Expression

正则表达式（Regular Expression，简称 regex 或 regexp）是一种用于匹配、查找、替换字符串的模式。几乎所有主流编程语言都支持正则表达式。

## 1. 基本语法

### 1.1 字面量匹配
- 普通字符直接匹配自身。  
  例如：`/abc/` 匹配字符串中的 "abc"。

### 1.2 元字符
| 元字符 | 描述 |
|--------|------|
| `.`    | 匹配除换行符以外的任意单个字符 |
| `^`    | 匹配字符串的开始（或行的开始，多行模式） |
| `$`    | 匹配字符串的结束（或行的结束） |
| `\`    | 转义符，使后面的元字符失去特殊含义 |
| `|`    | 逻辑“或”，匹配左边或右边的表达式 |
| `()`   | 分组，同时捕获匹配的内容 |
| `[]`   | 字符类，匹配方括号内的任意一个字符 |
| `{}`   | 量词，指定前面表达式出现的次数 |
| `*`    | 量词，匹配前面的表达式 0 次或多次 |
| `+`    | 量词，匹配前面的表达式 1 次或多次 |
| `?`    | 量词，匹配前面的表达式 0 次或 1 次；也用于使量词变为非贪婪 |

### 1.3 字符类简写
| 简写 | 含义                     | 等价于           |
|------|--------------------------|------------------|
| `\d` | 数字                     | `[0-9]`          |
| `\D` | 非数字                   | `[^0-9]`         |
| `\w` | 单词字符（字母、数字、下划线）| `[a-zA-Z0-9_]`   |
| `\W` | 非单词字符               | `[^a-zA-Z0-9_]`  |
| `\s` | 空白字符（空格、制表符等）| `[ \t\n\r\f\v]`  |
| `\S` | 非空白字符               | `[^ \t\n\r\f\v]` |
| `\b` | 单词边界                 | —                |
| `\B` | 非单词边界               | —                |

## 2. 量词

| 量词    | 含义（默认贪婪）           | 示例                         |
|---------|----------------------------|------------------------------|
| `{n}`   | 恰好 n 次                  | `\d{3}` 匹配 3 个数字        |
| `{n,}`  | 至少 n 次                  | `\w{2,}` 至少 2 个单词字符   |
| `{n,m}` | 至少 n 次，至多 m 次       | `[a-z]{2,4}` 2~4 个小写字母  |
| `*`     | 0 次或多次，等价于 `{0,}`  | `a*b` 匹配 "b"、"ab"、"aab"  |
| `+`     | 1 次或多次，等价于 `{1,}`  | `a+b` 匹配 "ab"、"aab"       |
| `?`     | 0 次或 1 次，等价于 `{0,1}`| `colou?r` 匹配 "color" 或 "colour" |

### 贪婪与非贪婪
- **贪婪模式**（默认）：尽量匹配更多字符。例如 `a.*b` 对 `"a123b456b"` 会匹配 `"a123b456b"`。
- **非贪婪模式**：在量词后加 `?`，尽量匹配更少字符。例如 `a.*?b` 对 `"a123b456b"` 会匹配 `"a123b"`。

## 3. 字符类 `[]`

- `[aeiou]`：匹配任意一个元音字母。
- `[0-9]`：匹配任意一个数字。
- `[a-zA-Z]`：匹配任意一个字母（大小写）。
- `[^abc]`：匹配除 a、b、c 以外的任意字符（`^` 在字符类开头表示取反）。

## 4. 分组与捕获

### 4.1 捕获分组 `()`
- 括号内的内容作为一个整体，并捕获匹配到的子串。
- 后续可通过 `$1`、`$2` 或 `\1`、`\2` 引用。

**示例**：`(\d{4})-(\d{2})-(\d{2})` 匹配日期 "2025-03-15"，捕获：
- $1 = "2025"
- $2 = "03"
- $3 = "15"

### 4.2 非捕获分组 `(?:)`
- 仅用于分组，不捕获内容，提高效率。
- 示例：`(?:http|https)://` 匹配协议但不会捕获。

### 4.3 命名分组 `(?<name>)` (部分语言支持)
- 示例：`(?<year>\d{4})`，可通过名称引用捕获内容。

### 4.4 反向引用
- 在正则表达式内部引用之前捕获的分组。例如 `(\w)\1` 匹配两个相同的单词字符（如 "aa"、"zz"）。

## 5. 断言（零宽断言）

断言匹配的是位置，不消耗字符。

### 5.1 前瞻断言
| 断言类型        | 语法       | 含义                         |
|----------------|------------|------------------------------|
| 正向前瞻        | `(?=...)`  | 当前位置之后必须匹配 ...     |
| 负向前瞻        | `(?!...)`  | 当前位置之后必须不匹配 ...   |

### 5.2 后顾断言 (部分语言支持)
| 断言类型        | 语法         | 含义                         |
|----------------|--------------|------------------------------|
| 正向后顾        | `(?<=...)`   | 当前位置之前必须匹配 ...     |
| 负向后顾        | `(?<!...)`   | 当前位置之前必须不匹配 ...   |

**示例**：
- `\d+(?=px)` 匹配数字后紧跟 "px" 的数字部分（如 "100px" 中的 "100"）。
- `(?<=\$)\d+` 匹配前面是 `$` 的数字（如 "$99" 中的 "99"）。

## 6. 标志（修饰符）

通常写在正则表达式的末尾，如 `/pattern/gim`。

| 标志 | 名称         | 作用                                                       |
|------|--------------|------------------------------------------------------------|
| `g`  | 全局匹配     | 查找所有匹配项，默认只返回第一个。                         |
| `i`  | 忽略大小写   | 匹配时不区分大小写。                                       |
| `m`  | 多行模式     | `^` 和 `$` 匹配每行的开始/结束（而不仅仅是整个字符串）。   |
| `s`  | 单行模式     | 使 `.` 匹配包括换行符在内的任意字符。                      |
| `u`  | Unicode      | 启用完整的 Unicode 支持（如处理四个字节的 Unicode 字符）。 |
| `y`  | 粘性匹配     | 从目标字符串的 `lastIndex` 位置开始匹配。                  |

## 7. 常用正则表达式示例

| 用途                     | 正则表达式                                      |
|--------------------------|-------------------------------------------------|
| 电子邮件                 | `^[\w\.-]+@[\w\.-]+\.\w+$`                      |
| 手机号（中国大陆）       | `^1[3-9]\d{9}$`                                 |
| 身份证（18位）           | `^[1-9]\d{5}(18\|19\|20)?\d{2}(0[1-9]\|1[0-2])(0[1-9]\|[12]\d\|3[01])\d{3}[\dXx]$` |
| IPv4 地址                | `^(25[0-5]\|2[0-4]\d\|1\d{2}\|[1-9]?\d)(\.(25[0-5]\|2[0-4]\d\|1\d{2}\|[1-9]?\d)){3}$` |
| 日期 YYYY-MM-DD          | `^\d{4}-(0[1-9]\|1[0-2])-(0[1-9]\|[12]\d\|3[01])$` |
| 时间 HH:MM:SS            | `^([01]\d\|2[0-3]):([0-5]\d):([0-5]\d)$`        |
| 匹配空行                 | `^\s*$`                                         |
| 中文字符                 | `[\u4e00-\u9fff]`                               |
| 用户名（字母开头，允许字母数字下划线，3-16位） | `^[a-zA-Z][a-zA-Z0-9_]{2,15}$`        |
| HTML 标签（简单）        | `<(\w+)[^>]*>.*?</\1>`                           |

## 8. 各语言中的使用示例

### JavaScript
```javascript
const regex = /\d+/g;
const str = "a1b2c3";
const matches = str.match(regex); // ["1", "2", "3"]
console.log(regex.test("123"));   // true
console.log("a1b2c3".replace(/\d/g, "X")); // "aXbXcX"